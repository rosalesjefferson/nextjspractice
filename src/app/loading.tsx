import Image from 'next/image';
// import loadingSpinner from '../public/loading-spinner.gif';

export default function LoadingSpinner() {
  return (
    <div className="spinner">
      {/* <Image src={loadingSpinner} alt="Loading..." width={50} height={50} /> */}
      LOADING.......
    </div>
  );
}
